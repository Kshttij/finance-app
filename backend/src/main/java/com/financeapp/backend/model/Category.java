package com.financeapp.backend.model;

/**
 * A simple enum to represent expense categories.
 */
public enum Category {
    FOOD,
    TRANSPORT,
    HOUSING,
    UTILITIES,
    ENTERTAINMENT,
    HEALTH,
    OTHER
}